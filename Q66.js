"use strict";
// Logical AND verification: Create a function that checks two boolean (True or False )values. It should only say true, using the && Operator. For instance, ceckbothTrue(True, false) should be false.
function checktwoboolean(value1, value2) {
    return value1 && value2;
}
console.log(checktwoboolean(true, false));
